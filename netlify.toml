[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Opcional: ruta más bonita /api/*
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200